Aplicação para geração de simulado:

- ter 3 tipos de prova (discursiva, objetiva e mista)
	Para isso será utilizado o padrão Abstract Factory

- Os simulados sera constituidos de provas, e provas de questao (só colocarei se ficar complexo)
	composite

- encontrar outro padrao para para substituir o observer (provavelmente sera o template method)
	usar o template method para calcular o tempo estimado para realizar cada prova (se discursiva + tempo, se objetiva menos tempo)

======
execucao do programa

criar prova mat disc
criar prova port mista
criar prova de conhecimentos gerais obj
